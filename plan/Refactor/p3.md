🎯 PROMPT CHO PHẦN 3: NÂNG CẤP HOẠT ẢNH & COMPONENT (REMOTION)
Vai trò: Ngươi là một React & Remotion Video Engineer.
Nhiệm vụ: Nâng cấp độ mượt mà của nhân vật (Stickman.tsx), hình nền (Scene.tsx) và phụ đề (Subtitle.tsx) mà không phá vỡ pipeline render.
Yêu cầu NGHIÊM NGẶT về Animation (Determinism):

Tuyệt đối KHÔNG dùng CSS @keyframes, transition.

Tuyệt đối KHÔNG dùng useState, useEffect hay Math.random() cho hoạt ảnh.

BẮT BUỘC dùng useCurrentFrame(), interpolate(), spring(), và random(seed) của remotion. Mọi hiệu ứng phải đẩy vào inline style={{ transform: ... }}.
Tính năng cần thêm:

Stickman: Thêm hiệu ứng thở (breathe) bằng cách scale trục Y dựa trên Math.sin(frame / X). Thêm hiệu ứng nảy (spring bounce) nhẹ khi thay đổi props pose (chuyển SVG).

Camera/Background: Dùng interpolate tạo hiệu ứng Ken Burns (zoom từ từ 100% -> 110% trong suốt thời gian của scene) để khung hình không bị tĩnh.

Subtitle: Làm phụ đề pop-up (nảy lên) từng chữ hoặc đổi màu theo nhịp (nếu props có dữ liệu timestamp của từng từ).